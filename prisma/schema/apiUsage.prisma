model ApiUsage {
  id       String   @id @default(cuid())
  userId   String
  endpoint String
  tokens   Int
  date     DateTime @default(now())
  user     User     @relation(fields: [userId], references: [id])
}
